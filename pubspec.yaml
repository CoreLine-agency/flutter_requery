name: flutter_requery
description: Flutter widget for fetching, caching and refetching asynchronous data.
version: 0.1.0
homepage: https://github.com/mislavlukach/flutter_requery
documentation: https://github.com/mislavlukach/flutter_requery

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=2.0.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
